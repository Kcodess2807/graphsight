"""TraceRAG — a local-first, headless GraphRAG curation & debugging layer.

Vectors and graph live natively in a single ``.lbug`` file (LadybugDB),
eliminating dual-database sync. See ``tracerag.config`` for all tunables.
"""

__version__ = "0.1.0"
